// Now you have to write a function that takes an argument and returns the
// square of it.

// Define square function... I would rather write a general one, what about you?

template <typename T>
T square(T argument)
{
    return argument * argument;
}
